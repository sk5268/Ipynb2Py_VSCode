// This is a minimal renderer implementation for the extension
// It's referenced in package.json to satisfy the notebookRenderer contribution

export function activate() {
    // This is an empty activation function for the renderer
    // The actual functionality is implemented in extension.ts
    console.log('Jupyter to Python Exporter renderer activated');
}